# 环境变量配置示例
# 
# 配置文件说明：
# - .env.local  : 本地开发使用（不提交 git，优先级最高）
# - .env.test   : 测试服和 Docker 构建使用（已创建）
# - .env.example: 配置文件模板（本文件）
#
# 本地开发：直接运行 npm run dev 会自动使用 .env.local
# Docker 构建：会自动使用 .env.test 配置

# API 地址
VITE_API_URL=http://localhost:8001

# 应用信息
VITE_APP_NAME=智能备忘录
VITE_APP_VERSION=1.0.0

# 环境
NODE_ENV=production
