# 智能备忘录应用

一个现代化的、响应式的备忘录应用，支持Web和Android平台。

## ✨ 功能特性

- 🔐 **用户登录** - 账号密码登录，支持记住密码功能
- 📝 **创建备忘录** - 添加标题、描述、到期时间
- ⏰ **智能提醒** - 可设置提前提醒时间（15分钟到1周）
- 📧 **邮件通知** - 后端到期时自动发送邮件提醒
- ✅ **任务管理** - 标记完成/未完成状态
- 📅 **延期功能** - 快速延期1天、3天、7天、14天、30天
- 🎨 **美观界面** - 现代化设计，支持响应式布局
- 📱 **多平台支持** - Web浏览器和Android App
- 🔍 **状态筛选** - 按全部/进行中/已完成/已过期筛选
- 💾 **本地存储** - 数据本地缓存，可扩展后端API

## 🛠️ 技术栈

- **React 18** - UI框架
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Lucide React** - 图标库
- **date-fns** - 日期处理
- **Capacitor** - 打包成原生App

## 📦 安装依赖

```bash
npm install
```

或使用 yarn:

```bash
yarn install
```

## 🚀 开发运行

启动开发服务器:

```bash
npm run dev
```

在浏览器中访问 `http://localhost:5173`

### 登录说明

当前为演示模式，输入任意用户名和密码即可登录。勾选"记住密码"后，下次访问会自动填充账号密码。

如需集成真实的用户认证系统，请修改 `src/utils/auth.js` 中的 `login` 函数，调用后端API进行验证。

## 📱 打包成Android App

### 1. 首次构建

```bash
# 构建Web应用
npm run build

# 添加Android平台
npx cap add android

# 同步文件到Android项目
npx cap sync android
```

### 2. 打开Android Studio

```bash
npm run android
```

这会自动打开Android Studio，然后你可以：
- 连接Android设备或启动模拟器
- 点击运行按钮进行调试
- 构建APK或AAB发布包

### 3. 后续开发流程

每次修改代码后：

```bash
npm run build
npx cap sync android
```

## 📂 项目结构

```
memo/
├── src/
│   ├── components/          # React组件
│   │   ├── Login.jsx       # 登录界面
│   │   ├── MemoForm.jsx    # 备忘录表单
│   │   └── MemoCard.jsx    # 备忘录卡片
│   ├── utils/              # 工具函数
│   │   ├── auth.js         # 用户认证
│   │   ├── storage.js      # 数据存储
│   │   └── dateHelpers.js  # 日期处理
│   ├── App.jsx             # 主应用组件
│   ├── main.jsx            # 入口文件
│   └── index.css           # 全局样式
├── public/                 # 静态资源
├── android/                # Android项目（首次构建后生成）
├── capacitor.config.json   # Capacitor配置
├── vite.config.js          # Vite配置
├── tailwind.config.js      # Tailwind配置
└── package.json            # 项目依赖
```

## 🔌 后端API集成

当前版本使用localStorage作为临时存储。要集成后端API，请修改 `src/utils/storage.js` 中的API函数。

### 需要的后端接口

```javascript
// 用户登录
POST /api/login
Body: { username, password }
Response: { token, username, userId }

// 获取所有备忘录
GET /api/memos
Headers: { Authorization: Bearer <token> }

// 创建备忘录
POST /api/memos
Headers: { Authorization: Bearer <token> }
Body: { title, description, dueDate, reminderMinutes, email }

// 更新备忘录
PUT /api/memos/:id
Headers: { Authorization: Bearer <token> }
Body: { status, dueDate, ... }

// 删除备忘录
DELETE /api/memos/:id
Headers: { Authorization: Bearer <token> }
```

### 后端需要实现的功能

1. **数据存储** - 将备忘录保存到数据库
2. **定时任务** - 检查到期的备忘录
3. **邮件发送** - 到期时发送邮件通知
4. **用户认证** - （可选）支持多用户

## 🎨 自定义配置

### 修改主题颜色

编辑 `tailwind.config.js`:

```javascript
theme: {
  extend: {
    colors: {
      primary: {
        // 修改这里的颜色值
      }
    }
  }
}
```

### 修改应用名称和图标

编辑 `capacitor.config.json`:

```json
{
  "appId": "com.yourcompany.memo",
  "appName": "你的应用名称"
}
```

## 📝 数据格式

备忘录数据结构:

```javascript
{
  id: "1234567890",
  title: "备忘录标题",
  description: "详细描述",
  dueDate: "2024-12-31T23:59:00.000Z",
  reminderMinutes: 30,
  email: "user@example.com",
  status: "pending", // pending | completed
  createdAt: "2024-01-01T00:00:00.000Z",
  updatedAt: "2024-01-01T00:00:00.000Z",
  completedAt: null
}
```

## 🐛 常见问题

### 1. Android打包失败

确保已安装：
- Java JDK 11或更高版本
- Android Studio
- Android SDK

### 2. 样式不生效

清除缓存并重新构建:

```bash
rm -rf node_modules dist
npm install
npm run build
```

### 3. 日期选择器不显示

某些浏览器的日期选择器样式可能不同，这是正常现象。

## 🚀 优化建议

1. **添加用户认证** - 支持多用户使用
2. **添加分类标签** - 为备忘录添加分类
3. **添加搜索功能** - 快速查找备忘录
4. **添加优先级** - 标记重要程度
5. **添加附件支持** - 上传图片/文件
6. **离线支持** - 使用Service Worker
7. **推送通知** - 使用Capacitor的本地通知插件

## 📄 开源协议

MIT License

## 👨‍💻 开发者

如有问题或建议，欢迎提Issue或PR！

---

祝你使用愉快！ 🎉
